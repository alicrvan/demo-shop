spring:
  datasource:
    url: jdbc:mysql://localhost/user-management?useSSL=false
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  security:
    user:
      name: root
      password: root